/**
 * Interruptores de funciones de e-commerce.
 * Cambiar a `true` para reactivar catálogo, carrito, auth, etc.
 */
export const featureFlags = {
  auth: false,
  cart: false,
  catalog: false,
  catalogFormSection: false,
  categoriesSection: false,
};
