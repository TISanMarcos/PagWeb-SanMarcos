# Copia a .env.local para probar Google Sheets en local:
#   cp .env.local.example .env.local
#
# Luego reinicia: npm run dev

VITE_WHATSAPP_NUMBER=525556943312

# URL CSV publicada (Archivo → Publicar en la web → CSV)
VITE_GOOGLE_SHEETS_PROMOTIONS_URL=https://docs.google.com/spreadsheets/d/e/TU_ID/pub?gid=0&single=true&output=csv

# Intervalo de sincronización en milisegundos (15000 = cada 15 segundos)
VITE_PROMOTIONS_POLL_MS=15000
