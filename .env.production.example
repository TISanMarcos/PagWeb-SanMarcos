# Variables de producción — copia a .env.production.local antes de npm run build
# NO subir .env.production.local al repositorio

VITE_WHATSAPP_NUMBER=525556943312

# Solo se usa al compilar (scripts/sync-promotions.mjs), NO queda en el JS público
VITE_GOOGLE_SHEETS_PROMOTIONS_URL=https://docs.google.com/spreadsheets/d/e/TU_ID/pub?output=csv
