import tailwindcss from 'tailwindcss';
import autoprefixer from 'autoprefixer';

/** Solo Tailwind v3 (tailwind.config.js). No usar @tailwindcss/postcss v4 aquí. */
export default {
  plugins: [
    tailwindcss,
    autoprefixer,
  ],
};
